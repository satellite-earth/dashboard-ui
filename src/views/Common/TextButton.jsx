import styled from '@emotion/styled';

const TextButton = styled.button`
	margin: 0;
	background: none;
	border: none;
	color: inherit;
	font-family: inherit;
	cursor: pointer;
	padding: 0;
`;

export default TextButton;
